package utils;

import models.Employee;

public class BillManager {

    // all employeee functions except for editing own info
    // are implemnted through this class
    /*
     * FUNCTIONALITY:
     *   - add new customer (upto three per id card)
     *   - all write access to customer
     *   - check expiry of cnic against nadra database
     *   - add rows in billing information about paid bills _> update the units consumed as well
     *   - update tax information
     *   - view current bill for customers
     *   - view reports of paid and unpaid bills (ammount of unpaid and paid bills)
     *   - view report of customers whose cnic is about to expire in the next 30 days
     *   - customers can update their id information in the nadra file
     */
}
